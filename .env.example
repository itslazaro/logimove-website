# LogiMove Logistics — environment template
# v1 is fully static; no required variables. Operational values live in src/config/site.ts.
# Add future secrets here (never commit real .env files).

# NEXT_PUBLIC_SITE_URL=https://logimove.example.com
